type Dog {
  name: String!
}

type Mutation {
  createDog(name: String!): Dog
}

type Query {
  # Means an array MUST be returned and all entries must be of type Dog i.e. no null etc.
  # dogs: [Dog!]!
  dogs: [Dog]!
}